@model IEnumerable<Infraestructure.Models.DetalleMovimiento>

@{
    ViewBag.Title = "GetDetallesMovimiento";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdDetalle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdDetalle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
